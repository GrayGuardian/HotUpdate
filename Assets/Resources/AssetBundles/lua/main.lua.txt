require("global")

--主入口函数。从这里开始lua逻辑
function Main() 
    print("Lua Main222")
end
--场景切换通知
function OnLevelWasLoaded(idx, name)
    collectgarbage("collect")
end
--游戏退出
function OnApplicationQuit()
end
