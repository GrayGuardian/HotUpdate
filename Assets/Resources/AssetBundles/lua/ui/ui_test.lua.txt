local base = require('ui_base')
local UI = class('ui_test', base)

UI.ASSET_INFO = {key = 'ui',asset = 'ui_test'};
UI.LOAD_ARG = {blockInput = false}

function UI:onStart(a,b)
    print(a,b);
    self.uiComp.Text.TipText.text = self.order .. "      "..a;
    -- self:close();
end
function UI:onButtonClick()
    print("点击按钮>>>>",self.uiComp.Text.TipText.text);
end
function UI:onDestroy()
    self.super:onDestroy();
    print("ui_test onDestroy");
end
return UI
